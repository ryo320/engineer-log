---
import AboutHeroImage from '../assets/aboutme.jpg';
import Layout from '../layouts/BlogPost.astro';
---

<Layout
	title="About Me"
	description="Logic in Code, Magic in Life. 0と1の世界を研ぎ澄まし、色彩豊かな人生を刻む。"
	pubDate={new Date('February 05 2026')}
	heroImage={AboutHeroImage}
	showComments={false}
>
	<div class="apple-style-about">
		<section class="hero-section">
			<p class="hero-tagline">Vision & Mission</p>
			<h2 class="hero-text">
				Logic in Code, Magic in Life.<br />
				<span class="gradient-text">0と1の世界を研ぎ澄まし、色彩豊かな人生を刻む。</span>
			</h2>
			<p class="lead-text">
				2026年よりITエンジニアとしての歩みを始める。
				<br> 仕事と趣味。論理と感性。
				<br> 互いを昇華させ、「より良い人生」を描く。
			</p>
		</section>

		<section class="content-section">
			<div class="section-label">Technical Focus</div>
			<div class="grid-layout">
				<div class="focus-card">
					<span class="card-status">Focus 01</span>
					<h3>Past</h3>
					<ul class="clean-list">
						<li>AI</li>
						<li>自律移動システム</li>
						<li>画像処理技術</li>
					</ul>
				</div>
				<div class="focus-card">
					<span class="card-status active">Focus 02</span>
					<h3>Current & Future</h3>
					<ul class="clean-list">
						<li>フロントエンド開発</li>
						<li>UI / UX デザイン</li>
					</ul>
				</div>
			</div>
		</section>

		<section class="content-section">
			<div class="section-label">Lifestyle & Passions</div>
			<div class="lifestyle-grid">
				<div class="life-group-card">
					<div class="group-icon">
						<svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
							<circle cx="12" cy="12" r="10" stroke-opacity="0.2"/>
							<polygon points="16.24 7.76 14.12 14.12 7.76 16.24 9.88 9.88 16.24 7.76" stroke="var(--accent-blue)"/>
						</svg>
					</div>
					<div class="life-content">
						<h4>Travel</h4>
						<p>シンガポール、ドイツ、台湾、イタリア、フランス、マカオ。2027年にオーロラ観測を計画中。</p>
					</div>
				</div>

				<div class="life-group-card">
					<div class="group-icon">
						<svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
							<path d="M3 18v-6a9 9 0 0 1 18 0v6" stroke="var(--accent-blue)"/>
							<rect x="17" y="13" width="4" height="7" rx="2"/>
							<rect x="3" y="13" width="4" height="7" rx="2"/>
						</svg>
					</div>
					<div class="life-content">
						<h4>Music</h4>
						<p>Vaundy、YOASOBI、ヨルシカ、back number、official髭男dismまで幅広く聴くミーハー。</p>
					</div>
				</div>

				<div class="life-group-card">
					<div class="group-icon">
						<svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
							<path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z" stroke="var(--accent-blue)" fill="var(--accent-blue)" fill-opacity="0.1"/>
						</svg>
					</div>
					<div class="life-content">
						<h4>Active</h4>
						<p>スノーボード、フルマラソン。ボルダリングとサーフィン、天体観測へ挑戦予定。</p>
					</div>
				</div>

				<div class="life-group-card">
					<div class="group-icon">
						<svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
							<path d="M17 11h1a3 3 0 0 1 0 6h-1" stroke-opacity="0.3"/>
							<path d="M7 21h10v-9.5c0-1.4-1.1-2.5-2.5-2.5h-5C8.1 9 7 10.1 7 11.5V21z" stroke="var(--accent-blue)"/>
							<path d="M12 9V5" stroke-opacity="0.3"/>
						</svg>
					</div>
					<div class="life-content">
						<h4>Culture</h4>
						<p>ドイツビールとビール工場見学。紅茶にも、はまりそうかも。</p>
					</div>
				</div>
			</div>
		</section>

		<button id="scroll-share-btn-about" class="share-trigger" onclick="shareAboutPage()">
			<svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M4 12v8a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2v-8"/><polyline points="16 6 12 2 8 6"/><line x1="12" y1="2" x2="12" y2="15"/></svg>
			<span>Share</span>
		</button>
	</div>

	<script is:inline>
		function shareAboutPage() {
			const title = 'Engineer Log | Logic in Code, Magic in Life.';
			const text = 'Engineer Log\nAbout me';
			const url = window.location.href;

			if (navigator.share) {
				navigator.share({ title, text, url });
			} else {
				navigator.clipboard.writeText(url);
				alert("リンクをコピーしました。シェアありがとうございます！");
			}
		}

		let lastScrollY = window.scrollY;
		let ticking = false;
		const shareBtn = document.getElementById('scroll-share-btn-about');

		window.addEventListener('scroll', () => {
			if (!ticking) {
				window.requestAnimationFrame(() => {
					const currentScrollY = window.scrollY;
					if (currentScrollY > lastScrollY && currentScrollY > 150) {
						shareBtn.classList.add('hidden');
					} else {
						shareBtn.classList.remove('hidden');
					}
					lastScrollY = currentScrollY;
					ticking = false;
				});
				ticking = true;
			}
		}, { passive: true });
	</script>

	<style>
		:root {
			--apple-black: #1d1d1f;
			--apple-gray: #86868b;
			--apple-border: rgba(0, 0, 0, 0.1);
			--accent-blue: #0071e3;
		}

		.apple-style-about {
			color: var(--apple-black);
			font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
			-webkit-font-smoothing: antialiased;
			position: relative;
		}

		/* Hero Title: ブログのh1とスケールを統一 (3.2rem) */
		.hero-section { padding: 4rem 0 5rem; text-align: center; }
		.hero-tagline { font-size: 0.9rem; font-weight: 700; text-transform: uppercase; letter-spacing: 0.2em; color: var(--apple-gray); margin-bottom: 1rem; }
		.hero-text { 
			font-size: 3.2rem; 
			font-weight: 900; 
			letter-spacing: -0.03em; 
			line-height: 1.1; 
			margin-bottom: 2rem; 
		}
		.gradient-text { background: linear-gradient(90deg, #0984e3, #6c5ce7); -webkit-background-clip: text; -webkit-text-fill-color: transparent; }
		
		/* Lead Text: 本文(1.1rem)より少しだけ大きく (1.2rem) */
		.lead-text { 
			font-size: 1.2rem; 
			line-height: 1.8; 
			color: #424245; 
			max-width: 680px; 
			margin: 0 auto; 
			font-weight: 500; 
		}

		.content-section { padding: 4rem 0; border-top: 1px solid var(--apple-border); }
		.section-label { font-size: 0.85rem; font-weight: 700; text-transform: uppercase; letter-spacing: 0.15em; color: var(--apple-gray); margin-bottom: 2.5rem; text-align: center; }

		.grid-layout, .lifestyle-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 2rem; }
		.lifestyle-grid { gap: 1.5rem; }

		.focus-card, .life-group-card {
			background: white;
			padding: 2.2rem;
			border-radius: 28px;
			border: 1px solid var(--apple-border);
			box-shadow: 0 4px 20px rgba(0, 0, 0, 0.03);
			transition: all 0.4s cubic-bezier(0.15, 0, 0.15, 1);
		}

		.focus-card:hover, .life-group-card:hover {
			transform: translateY(-8px);
			box-shadow: 0 20px 40px rgba(0, 0, 0, 0.08);
			border-color: var(--accent-blue);
		}

		.card-status { font-size: 0.75rem; font-weight: 800; color: var(--apple-gray); text-transform: uppercase; margin-bottom: 1rem; display: block; }
		.card-status.active { color: var(--accent-blue); }
		
		/* Card Titles: ブログのh3(1.5rem)に合わせる */
		.focus-card h3 { font-size: 1.5rem; font-weight: 800; margin-bottom: 1.2rem; letter-spacing: -0.01em; }

		.clean-list { list-style: none; padding: 0; }
		.clean-list li { font-size: 1.1rem; font-weight: 500; color: #424245; margin-bottom: 0.8rem; display: flex; align-items: center; }
		.clean-list li::before { content: ""; width: 6px; height: 6px; background: var(--accent-blue); border-radius: 50%; margin-right: 14px; opacity: 0.8; }

		.group-icon { margin-bottom: 1.2rem; color: var(--apple-black); }
		/* Lifestyle Subtitles: 本文との対比を明確に */
		.life-content h4 { font-size: 1.3rem; font-weight: 700; margin-bottom: 0.6rem; }
		.life-content p { font-size: 1.1rem; line-height: 1.6; color: #424245; font-weight: 400; }

		/* シェアボタンの滑らかなアニメーション（BlogPost.astroと完全同期） */
		.share-trigger {
			position: fixed;
			bottom: 2.5rem;
			right: 2.5rem;
			background: #ffffff;
			color: var(--apple-black);
			border: 2px solid var(--accent-blue);
			padding: 0.9rem 1.6rem;
			border-radius: 99px;
			font-weight: 700;
			display: flex;
			align-items: center;
			gap: 0.7rem;
			cursor: pointer;
			box-shadow: 0 15px 45px rgba(0, 113, 227, 0.15);
			z-index: 100;
			transition: 
				transform 0.4s cubic-bezier(0.4, 0, 0.2, 1), 
				opacity 0.3s ease-out,
				background 0.3s ease,
				color 0.3s ease;
			will-change: transform, opacity;
		}

		.share-trigger.hidden {
			transform: translateY(120px) scale(0.95);
			opacity: 0;
			pointer-events: none;
		}

		.share-trigger:hover {
			background: var(--accent-blue);
			color: #ffffff;
			transform: scale(1.05) translateY(-5px);
		}

		.share-trigger svg { color: var(--accent-blue); transition: color 0.3s ease; }
		.share-trigger:hover svg { color: #ffffff; }

		@media (max-width: 820px) {
			.hero-text { font-size: 2.4rem; }
			.lead-text { font-size: 1.1rem; }
			.grid-layout, .lifestyle-grid { grid-template-columns: 1fr; }
			.content-section { padding: 3rem 1.5rem; }
			.focus-card h3 { font-size: 1.4rem; }
			.share-trigger { bottom: 1.5rem; right: 1.5rem; padding: 0.8rem 1.3rem; font-size: 0.9rem; }
		}
	</style>
</Layout>