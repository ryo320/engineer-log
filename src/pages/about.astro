---
import AboutHeroImage from '../assets/aboutme.jpg';
import Layout from '../layouts/BlogPost.astro';
---

<Layout
	title="About Me"
	description="技術的関心とライフスタイル。制御理論からクリエイティブな開発へ。"
	pubDate={new Date('February 05 2026')}
	heroImage={AboutHeroImage}
	showComments={false}
>
	<div class="apple-style-about">
		<section class="hero-section">
			<p class="hero-tagline">Vision & Mission</p>
			<h2 class="hero-text">
				Logic in Code, Magic in Life.<br />
				<span class="gradient-text">0と1の世界を研ぎ澄まし、色彩豊かな人生を刻む。</span>
			</h2>
			<p class="lead-text">
				2026年よりITエンジニアとしての歩みを始める。
				<br> 仕事と趣味。論理と感性。
				<br> 互いを昇華させ、「より良い人生」を描く。
			</p>
		</section>

		<section class="content-section">
			<div class="section-label">Technical Focus</div>
			<div class="grid-layout">
				<div class="focus-card">
					<span class="card-status">Focus 01</span>
					<h3>Past</h3>
					<ul class="clean-list">
						<li>AI</li>
						<li>自律移動システム</li>
						<li>画像処理技術</li>
					</ul>
				</div>
				<div class="focus-card">
					<span class="card-status active">Focus 02</span>
					<h3>Current & Future</h3>
					<ul class="clean-list">
						<li>フロントエンド開発</li>
						<li>UI / UX デザイン</li>
					</ul>
				</div>
			</div>
		</section>

		<section class="content-section">
			<div class="section-label">Lifestyle & Passions</div>
			<div class="lifestyle-grid">
				<div class="life-group-card">
					<div class="group-icon">
						<svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
							<circle cx="12" cy="12" r="10" stroke-opacity="0.2"/>
							<polygon points="16.24 7.76 14.12 14.12 7.76 16.24 9.88 9.88 16.24 7.76" stroke="var(--accent-blue)"/>
						</svg>
					</div>
					<div class="life-content">
						<h4>Travel</h4>
						<p>シンガポール、ドイツ、台湾、イタリア、フランス、マカオ。2027年にオーロラ観測を計画中。</p>
					</div>
				</div>

				<div class="life-group-card">
					<div class="group-icon">
						<svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
							<path d="M3 18v-6a9 9 0 0 1 18 0v6" stroke="var(--accent-blue)"/>
							<rect x="17" y="13" width="4" height="7" rx="2"/>
							<rect x="3" y="13" width="4" height="7" rx="2"/>
						</svg>
					</div>
					<div class="life-content">
						<h4>Music</h4>
						<p>Vaundy、YOASOBI、ヨルシカ、back number、official髭男dismまで幅広く聴くミーハー。</p>
					</div>
				</div>

				<div class="life-group-card">
					<div class="group-icon">
						<svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
							<path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z" stroke="var(--accent-blue)" fill="var(--accent-blue)" fill-opacity="0.1"/>
						</svg>
					</div>
					<div class="life-content">
						<h4>Active</h4>
						<p>スノーボード、フルマラソン。ボルダリングとサーフィン、天体観測へ挑戦予定。</p>
					</div>
				</div>

				<div class="life-group-card">
					<div class="group-icon">
						<svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
							<path d="M17 11h1a3 3 0 0 1 0 6h-1" stroke-opacity="0.3"/>
							<path d="M7 21h10v-9.5c0-1.4-1.1-2.5-2.5-2.5h-5C8.1 9 7 10.1 7 11.5V21z" stroke="var(--accent-blue)"/>
							<path d="M12 9V5" stroke-opacity="0.3"/>
						</svg>
					</div>
					<div class="life-content">
						<h4>Culture</h4>
						<p>ドイツビールとビール工場見学。紅茶にも、はまりそうかも。</p>
					</div>
				</div>
			</div>
		</section>

		<button id="scroll-share-btn" class="share-trigger" onclick="sharePage()">
			<svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M4 12v8a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2v-8"/><polyline points="16 6 12 2 8 6"/><line x1="12" y1="2" x2="12" y2="15"/></svg>
			<span>Share</span>
		</button>
	</div>

	<script is:inline>
		// シェア機能
		function sharePage() {
			if (navigator.share) {
				navigator.share({
					title: 'Engineer Log | Logic in Code, Magic in Life.',
					text: 'Engineer Log',
					url: window.location.href,
				});
			} else {
				navigator.clipboard.writeText(window.location.href);
				alert("リンクをコピーしました。シェアありがとうございます！");
			}
		}

		// スクロール検知によるボタンの出現・消失ロジック
		let lastScrollY = window.scrollY;
		const shareBtn = document.getElementById('scroll-share-btn');

		window.addEventListener('scroll', () => {
			const currentScrollY = window.scrollY;

			if (currentScrollY > lastScrollY && currentScrollY > 200) {
				// 下スクロール時：隠す
				shareBtn.classList.add('hidden');
			} else {
				// 上スクロール時：表示
				shareBtn.classList.remove('hidden');
			}
			lastScrollY = currentScrollY;
		}, { passive: true });
	</script>

	<style>
		:root {
			--apple-black: #1d1d1f;
			--apple-gray: #86868b;
			--apple-border: rgba(0, 0, 0, 0.1);
			--apple-bg: #f5f5f7;
			--accent-blue: #0071e3;
		}

		.apple-style-about {
			color: var(--apple-black);
			font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
			-webkit-font-smoothing: antialiased;
			position: relative;
		}

		/* Hero Section */
		.hero-section {
			padding: 3.5rem 0 4.5rem;
			text-align: center;
		}
		.hero-tagline { font-size: 0.9rem; font-weight: 700; text-transform: uppercase; letter-spacing: 0.2em; color: var(--apple-gray); margin-bottom: 0.8rem; }
		.hero-text { font-size: 3.2rem; font-weight: 800; letter-spacing: -0.03em; line-height: 1.1; margin-bottom: 1.5rem; }
		.gradient-text { background: linear-gradient(90deg, #0984e3, #6c5ce7); -webkit-background-clip: text; -webkit-text-fill-color: transparent; }
		.lead-text { font-size: 1.25rem; line-height: 1.6; color: var(--apple-gray); max-width: 640px; margin: 0 auto; font-weight: 500; }

		/* Common Layout */
		.content-section { padding: 3rem 0; border-top: 1px solid var(--apple-border); }
		.section-label { font-size: 0.8rem; font-weight: 700; text-transform: uppercase; letter-spacing: 0.15em; color: var(--apple-gray); margin-bottom: 2.2rem; text-align: center; }

		/* Grid & Cards */
		.grid-layout, .lifestyle-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 2rem; }
		.lifestyle-grid { gap: 1.2rem; }

		.focus-card, .life-group-card {
			background: white;
			padding: 2.2rem;
			border-radius: 28px;
			border: 1px solid var(--apple-border);
			box-shadow: 0 4px 20px rgba(0, 0, 0, 0.03);
			transition: all 0.4s cubic-bezier(0.15, 0, 0.15, 1);
			display: flex;
			flex-direction: column;
		}

		.focus-card:hover, .life-group-card:hover {
			transform: translateY(-8px);
			box-shadow: 0 20px 40px rgba(0, 0, 0, 0.08);
			border-color: var(--accent-blue);
		}

		.card-status { font-size: 0.7rem; font-weight: 800; color: var(--apple-gray); text-transform: uppercase; margin-bottom: 0.8rem; display: block; }
		.card-status.active { color: var(--accent-blue); }
		.focus-card h3 { font-size: 1.7rem; font-weight: 800; margin-bottom: 1.2rem; letter-spacing: -0.01em; }

		.clean-list { list-style: none; padding: 0; }
		.clean-list li { font-size: 1.05rem; font-weight: 500; color: var(--apple-black); margin-bottom: 0.8rem; display: flex; align-items: center; }
		.clean-list li::before { content: ""; width: 6px; height: 6px; background: var(--accent-blue); border-radius: 50%; margin-right: 14px; opacity: 0.8; }

		.group-icon { margin-bottom: 1rem; color: var(--apple-black); }
		.life-content h4 { font-size: 1.1rem; font-weight: 700; margin-bottom: 0.5rem; color: var(--apple-black); }
		.life-content p { font-size: 1rem; line-height: 1.5; color: var(--apple-gray); font-weight: 400; }

		/* 改良：シェアボタンのデザイン（白基調のグラスモフィズム） */
		.share-trigger {
			position: fixed;
			bottom: 2rem;
			right: 2rem;
			/* 白い半透明背景とぼかし効果 */
			background: rgba(255, 255, 255, 0.8);
			backdrop-filter: blur(12px);
			-webkit-backdrop-filter: blur(12px);
			color: var(--apple-black);
			border: 1px solid rgba(0, 0, 0, 0.1);
			padding: 0.8rem 1.4rem;
			border-radius: 99px;
			font-weight: 600;
			display: flex;
			align-items: center;
			gap: 0.6rem;
			cursor: pointer;
			box-shadow: 0 10px 30px rgba(0,0,0,0.08);
			z-index: 100;
			transition: transform 0.4s cubic-bezier(0.15, 0, 0.15, 1), background 0.3s ease, border-color 0.3s ease;
		}

		.share-trigger.hidden {
			transform: translateY(150%) scale(0.8);
			pointer-events: none;
		}

		.share-trigger:hover {
			border-color: var(--accent-blue);
			background: rgba(255, 255, 255, 0.9);
			transform: scale(1.05) translateY(-5px);
		}

		/* アイコンの色をアクセントブルーに */
		.share-trigger svg {
			color: var(--accent-blue);
		}

		@media (max-width: 820px) {
			.hero-section { padding: 2.5rem 0 3.5rem; }
			.hero-text { font-size: 2.5rem; }
			.grid-layout, .lifestyle-grid { grid-template-columns: 1fr; }
			.content-section { padding: 2.5rem 1.5rem; }
			.life-group-card { padding: 1.5rem; align-items: center; text-align: center; }
			.share-trigger { bottom: 1.5rem; right: 1.5rem; }
		}
	</style>
</Layout>