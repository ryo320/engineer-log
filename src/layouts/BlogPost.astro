---
import { Image } from 'astro:assets';
import type { CollectionEntry } from 'astro:content';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import FormattedDate from '../components/FormattedDate.astro';
import Header from '../components/Header.astro';
import Comments from '../components/Comments.astro';
import LikeButton from '../components/LikeButton.astro';

type Props = CollectionEntry<'blog'>['data'];

const { title, description, pubDate, updatedDate, heroImage } = Astro.props;

const url = Astro.url.href;
const id = Astro.url.pathname;
---

<html lang="ja"> 
	<head>
		<BaseHead title={title} description={description} />
		<style is:global>
			/* ▼▼▼ 流体タイポグラフィ設定 ▼▼▼ */
			:root {
				font-size: clamp(15px, 0.4vw + 13.5px, 18px);
			}

			main {
				width: calc(100% - 2em);
				max-width: 100%;
				margin: 0;
			}
			.hero-image {
				width: 100%;
			}
			.hero-image img {
				display: block;
				margin: 0 auto;
				border-radius: 12px;
				box-shadow: var(--box-shadow);
			}

			.prose {
				width: 720px;
				max-width: calc(100% - 2em);
				margin: auto;
				padding: 1em;
				color: rgb(var(--gray-dark));
				font-size: 1rem;
				line-height: 1.8;
			}

			/* ▼▼▼ 修正点：タイトルエリアの隙間を圧縮 ▼▼▼ */
			.title {
				margin-bottom: 0.5em; /* 2em から大幅に短縮 */
				padding: 0.5em 0 0 0; /* paddingも控えめに */
				text-align: center;
				line-height: 1.2;
			}
			.title h1 {
				margin: 0 0 0.2em 0; /* 0.5em から短縮 */
				font-size: 2.8rem;
				font-weight: 900;
				color: #2d3436;
			}

			/* いいねボタンのコンテナ隙間調整 */
			.like-button-wrapper {
				display: flex;
				justify-content: center;
				margin: 0.2rem 0; /* 1.5rem から大幅に短縮 */
			}

			/* LikeButton内部のマージンを外部から上書きして強制的に詰める */
			.like-button-wrapper :global(.like-container) {
				margin: 0 !important; 
				gap: 4px !important;
			}

			/* タイトル下の区切り線の余白を調整 */
			.title hr {
				margin: 0.5rem auto 1.5rem; /* 上下の隙間を最適化 */
				border: 0;
				border-top: 1px solid #eee;
			}

			.prose h2 {
				font-size: 1.8rem;
				margin: 2rem 0 0.8rem; /* 少し詰め気味に */
			}
			.prose h3 {
				font-size: 1.6rem;
				margin: 1.5rem 0 0.8rem;
			}

			.date {
				margin-bottom: 0.3em; /* 短縮 */
				color: rgb(var(--gray));
			}
			.last-updated-on {
				font-style: italic;
			}
			
			.comment-divider {
				border: 0;
				border-top: 1px solid #eee;
				margin: 3rem 0 2rem; /* 少し短縮 */
			}

			@media (max-width: 768px) {
				.title h1 {
					font-size: 2rem; /* スマホで大きすぎないように調整 */
					line-height: 1.3;
					padding: 0 0.2rem;
				}
				.prose {
					padding: 0.5em;
				}
			}
		</style>
	</head>

	<body>
		<Header />
		<main>
			<article data-pagefind-body>
				<div class="hero-image">
					{heroImage && <Image width={1020} height={510} src={heroImage} alt="" />}
				</div>
				<div class="prose">
					<div class="title">
						<div class="date">
							<FormattedDate date={pubDate} />
							{
								updatedDate && (
									<div class="last-updated-on">
										Last updated on <FormattedDate date={updatedDate} />
									</div>
								)
							}
						</div>
						<h1>{title}</h1>

						<div class="like-button-wrapper">
							<LikeButton slug={id} />
						</div>

						<hr />
					</div>
					<slot />
				</div>
			</article>

			<div class="prose">
				<hr class="comment-divider" />
				<section>
					<h3 style="margin-bottom: 1.5rem;">Comments</h3>
					<Comments title={title} id={id} url={url} />
				</section>
			</div>
		</main>
		<Footer />
	</body>
</html>