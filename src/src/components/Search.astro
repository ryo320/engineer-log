<link href="/pagefind/pagefind-ui.css" rel="stylesheet">
<script src="/pagefind/pagefind-ui.js" is:inline></script>

<div id="search" class="search-container"></div>

<script>
  window.addEventListener('DOMContentLoaded', () => {
    // @ts-ignore
    new PagefindUI({
      element: "#search",
      showSubResults: true,
      pageSize: 5,
      translations: {
        placeholder: "キーワードで記事を検索...",
        zero_results: "「[SEARCH_TERM]」に一致する記事は見つかりませんでした。"
      }
    });
  });
</script>

<style is:global>
  /* --- Pagefind デザインカスタマイズ --- */
  :root {
    /* 藤本さんのブログのテーマカラーに合わせる */
    --pagefind-ui-primary: #0984e3; 
    --pagefind-ui-text: #2d3436;
    --pagefind-ui-background: #ffffff;
    --pagefind-ui-border: #dfe6e9;
    --pagefind-ui-tag: #f4f7f6;
    --pagefind-ui-border-radius: 12px;
    --pagefind-ui-font: 'Inter', sans-serif;
  }

  /* 検索窓自体のサイズを絞って「デカすぎ」を解消 */
  .search-container {
    width: 100%;
    max-width: 600px;
    margin: 0 auto 3rem auto;
  }

  /* 入力フォームのおしゃれ化 */
  .pagefind-ui__search-input {
    font-weight: 500 !important;
    border: 2px solid #dfe6e9 !important;
    transition: all 0.3s ease !important;
    box-shadow: 0 4px 6px rgba(0,0,0,0.02) !important;
  }

  .pagefind-ui__search-input:focus {
    border-color: #0984e3 !important;
    box-shadow: 0 8px 15px rgba(9, 132, 227, 0.1) !important;
    outline: none;
  }

  /* 検索結果のカードをブログのリスト風に */
  .pagefind-ui__result {
    border: none !important;
    background: #fff !important;
    border-radius: 12px !important;
    margin-bottom: 1rem !important;
    padding: 1rem !important;
    transition: transform 0.2s ease !important;
    box-shadow: 0 2px 8px rgba(0,0,0,0.05) !important;
  }

  .pagefind-ui__result:hover {
    transform: translateY(-2px);
    box-shadow: 0 5px 15px rgba(0,0,0,0.1) !important;
  }

  /* 「すべて表示」ボタンをかっこよく */
  .pagefind-ui__button {
    background: #f4f7f6 !important;
    border: none !important;
    color: #0984e3 !important;
    font-weight: 600 !important;
    border-radius: 50px !important;
    margin-top: 1.5rem !important;
  }
</style>